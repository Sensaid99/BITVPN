# Журнал сессий с Cursor

Файл для сохранения контекста переписок и сделанных правок. При новых задачах агент читает этот файл и дополняет его после работы.

---

## Как пользоваться

- **Перед задачей:** агент смотрит последние записи, чтобы не повторять ошибки и учитывать уже сделанное.
- **После правок:** агент добавляет краткую запись: дата, что сделано, какие файлы затронуты, что проверить или что ещё может быть не так.

---

## 2026-03-27 — /start не реагирует; приоритет хендлера; 3x-ui изоляция

### Запрос
После смены токена кнопка «Старт» не реагирует; проверка на нестыковки; инструкция по 3x-ui для отдельного учёта на пользователя.

### Что сделано
- **`bot/main.py`**: `CommandHandler('start')` в **group=-2** (выше callback и `ConversationHandler`), чтобы `/start` обрабатывался первым; в **`error_handler`** игнор уведомлений админам при **`Forbidden`** (бот заблокирован пользователем).
- **`bot/handlers/main.py`**: при входе в **`start_command`** — **INFO**-лог `update_id`, `chat_id`, `user_id`, `args`; **`except Exception`** после `DataError` — уведомление админам + короткий текст пользователю при сбое.
- **`deploy/vpn-bot.service`**: комментарий про `.env` в `WorkingDirectory`.
- **`docs/deploy/3X_UI_ИЗОЛЯЦИЯ_ПОЛЬЗОВАТЕЛЕЙ.md`**, строка в **`docs/README.md`**.

### Что проверить на сервере
`journalctl -u vpn-bot -f` — при нажатии Старт должна появиться строка `start_command: update_id=...`. Если строки нет — апдейты не доходят (другой процесс с тем же токеном, webhook, сеть). Если строка есть, но нет ответа — смотреть исключение в логе.

---

## 2026-03-27 — BotFather; две ноды в Happ; /start; отладка /sub

### Запрос
Пошагово что менять в BotFather; клиент видит одну ноду вместо двух и «32 ГБ» на новой подписке; /start не отвечает.

### Что сделано в коде
- **`bot/handlers/main.py`**: имя в приветствии через **`escape_html`** (спецсимволы в имени Telegram ломали HTML → **BadRequest**, бот молчал); при ошибке parse_mode — повтор без HTML.
- **`bot/config/settings.py`**: **`HAPP_SUBSCRIPTION_URLS`** в Config (как в `.env`).
- **`api_miniapp.py`**: вынесены **`_happ_subscription_upstream_bases`**, **`_build_sub_install_target`**; **`debug-sub-content`** считает ноды по каждому upstream и после склейки; **`check-happ_env`**: учёт «одна URL или список URLS».

### Инфра (не код)
- **Две ноды в Happ:** либо в **3x-ui** у одной подписки несколько inbound, либо в **`.env` на API** `HAPP_SUBSCRIPTION_URLS=url1,url2` — тогда `/sub/CODE` склеивает ответы (уже было в коде).
- **32 ГБ:** счётчик трафика задаёт **панель Xray/3x-ui и/или Happ** по клиенту/inbound; бот не выставляет «гигабайты». Проверить в панели: отдельный email/клиент на пользователя, не общий inbound.

### Проверить
`/api/miniapp/debug-sub-content?install_code=...` при `MINIAPP_EXPOSE_DEBUG=1`; в браузере открыть выдачу `https://домен/sub/CODE` (base64 → несколько `vless://`).

---

## 2026-03-27 — дубли приветствия после рестарта бота

### Причина
В `post_init` вызывался `delete_webhook(drop_pending_updates=False)`. Старые `/start` могли оставаться в очереди Telegram и после редеплоя/рестарта обрабатываться снова — пользователю уходило приветствие без нового нажатия «Старт».

### Что сделано
- **`bot/main.py`**: `delete_webhook(drop_pending_updates=True)` + комментарий. `run_polling(..., drop_pending_updates=True)` уже был — теперь первый вызов не оставляет очередь.

### Проверить
Несколько рестартов `vpn-bot` без нажатия Старт у клиента — приветствие не должно повторяться. Сообщения о запуске/остановке бота — только в `ADMIN_IDS`.

### Если дубли остаются
Проверить, что не запущено **несколько процессов** с одним токеном (дубли systemd, лишний screen/docker).

---

## 2026-03-27 — логи: BadRequest «Query is too old», JobQueue, токен в journalctl

### Что видно в логах
- **`BadRequest: Query is too old...`** в `setup_device_handler` при `answerCallbackQuery` — типично после рестарта бота, когда в очереди остался старый callback.
- **`JobQueue` / `run_daily`**: без extra **`[job-queue]`** у PTB `job_queue` = `None`, джоба с напоминаниями об истечении подписки не ставилась.
- В **`journalctl`** при уровне INFO у **httpx** в URL попадает **токен бота** — логи лучше не публиковать целиком; при утечке — **новый токен в @BotFather**.

### Что сделано
- **`bot/handlers/main.py`**: `_answer_callback_safe` для всех `query.answer` — просроченный callback игнорируется (debug), хендлер не падает.
- **`bot/main.py` `error_handler`**: тот же `BadRequest` не шлёт длинный traceback админам.
- **`post_init`**: если `job_queue is None` — предупреждение в лог и подсказка про `pip install "python-telegram-bot[job-queue]"`.
- **`requirements.txt`**: `python-telegram-bot[job-queue]==20.7`.

### Проверить на сервере
`pip install -r requirements.txt` (или отдельно extra), рестарт `vpn-bot`, в логе должна появиться строка про Job expiry; устаревшая кнопка после рестарта — без исключения и без спама админам.

---

## 2026-03-27 — PostgreSQL: новый пользователь Telegram, INSERT падает (NumericValueOutOfRange)

### Причина
`users.telegram_id` был **INTEGER** (max 2 147 483 647). У Telegram уже есть **ID > 2³¹−1** (пример: 8013285688) — при первом `/start` INSERT в PostgreSQL падал с `NumericValueOutOfRange`, бот не отвечал.

### Что сделано
- В **`bot/models/database.py`**: колонка **`telegram_id` → `BigInteger`**; при старте для PostgreSQL выполняется **`ALTER TABLE users ALTER COLUMN telegram_id TYPE BIGINT`**, если тип был integer.

### Проверить
После деплоя перезапуск `vpn-bot`, повторный `/start` от пользователя с большим ID; при необходимости миграция вручную в Neon.

### Дополнение (если всё ещё «тишина»)
- Миграция через `current_schema()` могла не тронуть колонку — заменено на **`DO $$` + `public.users`** и исправлен синтаксис `END;` в PL/pgSQL.
- В **`/start`**: `effective_message`, перехват **`sqlalchemy.exc.DataError`**, ответ пользователю + лог с подсказкой про BIGINT.
- Ручной SQL: **`deploy/postgres-alter-telegram-id-bigint.sql`**.

### 2026-03-27 — без консоли Neon: миграция BIGINT из кода; ошибки только админам
- **`users.telegram_id`**: миграция без PL/pgSQL — `SELECT information_schema` → при необходимости `ALTER … USING (telegram_id::bigint)` (можно без ручного Neon).
- **`bot/main.py` `error_handler`**: клиентам **не** отправляются тексты про временные/технические ошибки; полный лог уходит **`notify_admins`** → `ADMIN_IDS`.
- **`/start` `DataError`**: сообщение пользователю убрано; **`notify_admins`** с пояснением.
- **`bot/utils/telegram_notify.py`**: `notify_admins`.

---

## 2026-03-27 — бот «молчит» на /start: webhook vs polling

### Причина
На токене бота мог остаться **webhook** (тесты, другой сервер, BotFather). В режиме **long polling** апдейты тогда не приходят — пользователь нажимает Старт, а ответа нет.

### Что сделано
- В **`bot/main.py`** (`post_init`): перед инициализацией БД вызываются `get_webhook_info` и **`delete_webhook`**, при активном webhook пишется предупреждение в лог.
- **`bot/handlers/main.py`** — **`get_webapp_url()`**: если задан `MINIAPP_API_URL` и в `WEBAPP_URL` ещё нет `api=`, параметр добавляется (иначе мини-апп мог не находить API).

### Проверить на сервере
`systemctl status vpn-bot`, `journalctl -u vpn-bot -n 80`, ручной вызов `getWebhookInfo` в браузере/curl с токеном.

---

## 2026-03-27 — лендинг: структура главной, модалки «Тарифы / Инструкция / Кабинет»

### Запрос
Убрать тарифы и регистрацию с главной; сверху кнопки «Тарифы» (срок + устройства, как в боте, с parallax), «Инструкция», «Личный кабинет» (модалка вход/регистрация вместо прямого Vercel); регистрация только по Telegram username, почта/телефон — во вкладке «Профиль» в кабинете; синхронизация с ботом — в кабинете; поддержка — внизу страницы.

### Что сделано
- **`website/index.html`**: переработан главный экран (описание, «почему мы», сеть + платформа, возможности); убран дубликат HTML в конце файла; верхняя панель: Тарифы / Инструкция / Личный кабинет / Открыть бот; модальное окно тарифов с parallax-орбами, выбором устройств и срока, расчётом цены как в мини-аппе, кнопка оплаты в боте (`pay_{months}month_{devices}`); модалка инструкции (3 шага); модалка кабинета: вкладки Вход (ссылки на веб-кабинет и бота), Регистрация (только username), Профиль (email/телефон, синхронизация с ботом); поддержка в блоке в футере.
- **`api_miniapp.py`**: `POST /api/site/register` — обязателен только `tg_username`; email и phone опциональны; в JSONL добавлено поле `event` (`register` / `contact`).

### Проверить
Деплой на VPS, `POST /api/site/register` с одним username; заполнение профиля с email/phone; открытие тарифов при доступном `/api/miniapp/plans`.

---

## 2026-03-27 — лендинг не деплоился: `website/` не был в репозитории

### Причина
- `website/index.html` лежал только локально и был **untracked** — в GitHub не попадал, на VPS после `git pull` файла не было (`ls: cannot access 'website/index.html'`). Тогда `api_miniapp` отдаёт следующий fallback — мини-приложение/заглушку («Bit VPN»).
- Ветка `master` на remote отставала от `main`; серверы часто делают `git pull` по `master`.

### Что сделано
- Закоммичен и запушен `website/index.html`; ветка `master` на GitHub обновлена (fast-forward к `main`), чтобы обычный `git pull` на сервере подтянул лендинг.

### Проверить на VPS
`cd /opt/vpn-bot && git pull && ls -la website/index.html && sudo systemctl restart miniapp-api` — затем в браузере обновить страницу без кэша (Ctrl+F5).

### Если после pull всё ещё «VPN — Быстро и безопасно» (webapp)
- В логах при старте/первом запросе: `serve_webapp: /opt/vpn-bot/website/index.html` или предупреждение, что нет файлов.
- В `.env` можно указать `WEBSITE_INDEX_PATH=/opt/vpn-bot/website/index.html`.
- В коде: базовый каталог через `os.path.realpath(__file__)`, чтобы symlink не ломал путь к `website/`.

---

## 2026-03-25 — запуск полноценного сайта на домене

### Запрос
Сделать отдельный современный сайт на `bitecosystem.ru` (не только mini app).

### Что сделано
- Добавлен новый лендинг: `website/index.html` в фирменной тёмной стилистике (matrix-fon, parallax, mockup, адаптив), тексты с акцентом на «Безопасный интернет».
- Кнопки синхронизированы с действующим ботом/кабинетом; цены и username поддержки/бота подтягиваются из `GET /api/miniapp/plans`.
- Добавлен endpoint `POST /api/site/register` (tg_username, email, phone) с валидацией и сохранением в `logs/site_leads.jsonl`.
- В `api_miniapp.py` приоритет раздачи корня `/`: сначала `website/index.html`, затем fallback на `webapp/public`.

### Проверить
Задеплоить код на сервер `213.165.38.222`, перезапустить `miniapp-api`, открыть `https://bitecosystem.ru`.

---

## 2026-03-25 — одна подписка, несколько серверов (3x-ui)

### Запрос
Сделать так, чтобы по одной подписке было несколько серверов (добавить Финляндию к BIT VPN).

### Что сделано
- **`docs/deploy/ОДНА_ПОДПИСКА_НЕСКОЛЬКО_СЕРВЕРОВ_3XUI.md`**: мастер = панель из `HAPP_SUBSCRIPTION_URL` (95.181), Финляндия как **нода**; проверка — в ответе подписки несколько `vless://`. **`docs/README.md`**, ссылка в **`docs/ТРИ_СЕРВЕРА_В_HAPP_ПОШАГОВО.md`**.

### Проверить
В 3x-ui на 95.181 настроить Nodes к панели FI; в браузере открыть базовую подписку мастера — 2+ ноды. Ранее: текст про TUN в мини-аппе (`webapp/index.html` + копии).

---

## 2026-03-17 — документ: проверка ссылок подписки

### Запрос
Пошагово закрыть тему: как везде проверить старую/новую ссылку, что прописать, какие команды.

### Что сделано
- Добавлен **`docs/deploy/ПРОВЕРКА_ССЫЛОК_ПОДПИСКИ_ПОЛНЫЙ_ЧЕКЛИСТ.md`**: роли URL (панель vs `bitecosystem.ru/sub/`), таблица `.env`, SSH/`curl`, BotFather, Vercel, чеклист в конце. В **`docs/README.md`** — строка в оглавление.

---

## 2026-03-17 — убран блок «Сейчас в Happ подключено» на главной

### Запрос
Убрать с главного экрана карточку «Сейчас в Happ подключено: N / M».

### Что сделано
- Удалены разметка `#main-devices-summary`, стили `.main-devices-summary*` и код в `renderSubsAndPayFromApi`. Файлы: `webapp/index.html`, `public/index.html`, `index.html`, `api/root_index.html`.

### Проверить
Деплой мини-аппа на Vercel.

---

## 2026-03-17 (вторник)

### Запрос
После «Добавить» (автопереход в Happ) мини-апп сбрасывается и показывает «Ошибка загрузки»; сомнение, что в Happ подставляется «не та» ссылка/настройки по сравнению с ручным копированием.

### Что сделано
- Уточнение по коду: в Happ уходит **та же** `subscription_link`, что из `/api/miniapp/me`, только обёрнутая в `happ://add/...` и открытие через `/api/miniapp/redirect-to-app`; отдельных «настроек» фронт не подставляет.
- **«Ошибка загрузки»** — это `API_ME` не загрузился (`/api/miniapp/me` не ok), не сообщение о неверной ссылке VPN.
- В `openHappAddSubscription` при наличии **Telegram WebApp** сначала вызывается **`tg.openLink(redirectUrl)`**, чтобы не уводить мини-апп с `location.href` / не терять контекст после возврата (раньше при блокировке `window.open` срабатывал переход и сброс).

### Файлы
`webapp/index.html`, `public/index.html`, `index.html`, `api/root_index.html`

### Проверить
Деплой мини-аппа на Vercel; в Telegram: «Добавить» → возврат в мини-апп без «Ошибка загрузки». Если ошибка остаётся — смотреть `?api=`, CORS, ответ сервера `/api/miniapp/me`.

---

## 2026-03-22 (воскресенье)

### Запрос
При нажатии «Добавить подписку» в мини-аппе (экран установки клиента) показывать промежуточное меню с пояснением и кнопками «Добавить» / «Назад»; свой текст и оформление (не копия конкурента).

### Что сделано
- После «Добавить подписку» закрывается лист установки и открывается модалка `#add-sub-hint-overlay` с заголовком, текстом и кнопками; по «Добавить» — прежняя логика (копирование + `showLinkCopiedModal` / `openSetupSheet` / алерт); «Назад» и клик по фону — закрытие и возврат к листу установки клиента.
- Стили: `.add-sub-hint-*` (градиент, акцент BIT VPN). Синхронизировано: `webapp/index.html`, `public/index.html`, `index.html`, `api/root_index.html`.

### Проверить
- Деплой мини-аппа на Vercel; в Telegram открыть сценарий установки → «Добавить подписку» → новое окно → «Добавить» → инструкция со ссылкой.

### Дополнение (тот же день)
- По «Добавить» в промежуточном окне вызывается **`openHappAddSubscription`** (редирект через `/api/miniapp/redirect-to-app` → `happ://add/...`), fallback — `showLinkCopiedModal`.
- На **главном экране** блок **`#main-devices-summary`**: «Сейчас в Happ подключено: N / M» при активной подписке и данных от API; в **разделе Подписки** в карточке активной подписки снова выводится строка **«Устройств:»** при динамическом рендере.

---

## 2025-03-17 — документация деплоя (bitecosystem.ru)

### Запрос
Единый продакшен: домен **bitecosystem.ru**, IP сервера бота/API **213.165.38.222**; обновить примеры в репозитории и дать гайд с нуля (бот + мини-апп + Vercel).

### Что сделано
- Массовая замена старых примеров (`nikolay.lisobyk.fvds.ru`, `155.212.164.135`) на **bitecosystem.ru** / **213.165.38.222** в `docs/`, `deploy/*.conf`, `deploy/apply-nginx-default.sh` и др.
- Добавлен **`docs/deploy/BITEcosystem_SERVER_VERCEL.md`**; **`docs/README.md`** — строка в оглавление; **`.env.example`** — пример с bitecosystem.ru и bitvpn.vercel.app.
- Скрипт **`tools/_bulk_replace_deploy_examples.ps1`** — для повторной синхронизации примеров при необходимости.
- IP панели подписки VPN (**95.181.175.67**) в текстах, где это «отдельный сервер 3x-ui», не подменялся на 213.165.38.222 — это разные роли.

---

## 2025-03-17 (вторник)

### Запрос пользователя

- **Рефералка «Как это работает?»:** описание (текст и список) спрятать за кружочком «i» — показывать только после нажатия на него.

### Что сделано

- В блоке рефералки заголовок сделан кнопкой: кружок «i», текст «Как это работает?», стрелка вниз. Параграф и список из трёх пунктов перенесены в блок `.ref-how-body`, по умолчанию скрыт (`display: none`). По клику на кнопку переключается класс `ref-how-open` на карточке — описание показывается/скрывается, стрелка поворачивается. Добавлены `aria-expanded` и `aria-controls` для доступности.
- **Файлы:** `webapp/index.html`, `public/index.html`, `api/root_index.html`, `index.html` (разметка, стили, обработчик клика).

### Ещё (тот же день)

- **Панель «Устройства»:** две кнопки «Скопировать ссылку» и «Заменить ссылку» заменены на одну «+ Добавить устройство»; по нажатию копируется ссылка и открывается модалка с пошаговой инструкцией и кнопкой «Скопировать ссылку». Описание «если VPN не работает» (замена ссылки в Happ + режим TUN) вынесено в отдельный раскрывающийся блок «VPN не работает?» — по умолчанию свёрнут, по клику показывается текст. Текст внутри блока обновлён: «скопируйте ссылку кнопкой «Добавить устройство»…».
- **Файлы:** те же четыре `index.html` (разметка, стили, JS).
 - **Правка UI:** кнопка «+ Добавить устройство» перенесена вниз к списку устройств; старая нижняя кнопка удалена вместе с обработчиком.
 - **Откат:** изменения оттенка зелёного (accent/градиенты/rgba) отменены — вернули исходные цвета.
 - **Заметка:** в экране «Устройства» счётчик/список показывает `0 / 10` и «Нет устройств» (скрин от пользователя). К задаче серверов вернуться позже.
- **Профиль — «Мой конфиг»:** в экране «Профиль» мини-аппа добавлена строка «Мой конфиг» (иконка шестерёнки, подзаголовок «Получить ссылку и настроить VPN»). По нажатию открывается бот с `?start=config` (через `tg.openTelegramLink` или `window.open`). **Файлы:** `webapp/index.html`, `public/index.html`, `api/root_index.html`, `index.html` (разметка + JS: переменная `profileLinkMyConfig`, обработчик клика). Бот уже обрабатывает `/start config` в `bot/handlers/main.py` — правок в боте не требуется.
- **Счётчик «Подключено» 0/10 после подключения в Happ:** добавлена переменная **HAPP_LIST_INSTALL_URL**. По официальной документации Happ базовый URL API — **https://happ-proxy.com** (не api.). В проекте дефолт HAPP_API_URL=https://happ-proxy.com; добавлен **docs/happ/API_ОФИЦИАЛЬНЫЙ_URL.md**; скрипт add-domain переведён на HAPP_API_URL из .env.
- **ProviderID (документация Happ):** для учёта устройств и проверки подписки приложение должно получать ProviderID (= HAPP_PROVIDER_CODE). В ответе **GET /sub/{install_code}** при проксировании контента добавлены: заголовок `providerid: <HAPP_PROVIDER_CODE>` и комментарий в теле подписки `#providerid <HAPP_PROVIDER_CODE>`. Описание — в **docs/happ/API_ОФИЦИАЛЬНЫЙ_URL.md** (раздел Provider ID).
- **Деплой — чтобы везде работало:** проверены все пути (бот, API miniapp_me, webhook, debug-install-stats, скрипты) — везде используется HAPP_API_URL из Config/.env или fallback https://happ-proxy.com. Обновлены docs/happ/ПРОВЕРКА_ССЫЛКИ_HAPP.md, docs/deploy/ДЕПЛОЙ_И_ГОТОВНОСТЬ_ПРОДУКТА.md, docs/deploy/ДЕПЛОЙ.txt (чеклист перед деплоем: HAPP_API_URL, MINIAPP_API_URL, COPY_ENV_TO_SERVER). Эндпоинт `list-install` (счётчик устройств) может быть только на `https://happ-proxy.com`, а выдача ссылок (add-install) — на `https://api.happ-proxy.com`. Теперь для запросов list-install используется HAPP_LIST_INSTALL_URL, если задан, иначе HAPP_API_URL. **Что сделать на сервере:** в `.env` задать `HAPP_LIST_INSTALL_URL=https://happ-proxy.com`, перезапустить miniapp-api. **Файлы:** `bot/config/settings.py`, `api_miniapp.py` (miniapp_me, debug_install_stats), `.env.example`, `docs/happ/ПРОВЕРКА_ССЫЛКИ_HAPP.md`. В ответ debug-install-stats добавлено поле `list_install_url_used`.

---

## 2025-03-15 (воскресенье)

### Запросы пользователя

1. **Устройство подключено, но не отображается в «Подключено»**  
   В мини-аппе в разделе «Устройства» было «0» и «Нет устройств», хотя пользователь подключил устройство.

2. **Ссылка подписки без installid**  
   Апп выдавал ссылку вида `https://95.181.175.67:2096/sub_bitvpn/bgmdn1s016p08yfb` (базовый URL без `?installid=...`).

3. **Проверка настройки Happ**  
   Как проверить переменные и работу API.

4. **404 при запросе к Happ API**  
   `https://happ-proxy.com/api/add-install` с сервера и из браузера возвращал 404.

5. **Поддержка прислала URL и документацию**  
   Документация указывала на `https://happ-proxy.com`, но с заголовком `Accept: application/json` 404 сохранялся.

6. **Проверка api.happ-proxy.com**  
   Запрос к `https://api.happ-proxy.com/api/add-install` вернул JSON — API доступен по поддомену `api.`.

7. **Просьба сохранять переписки и дела**  
   Создать файл-журнал и при новых задачах опираться на него.

### Что сделано

- **Счётчик «Подключено»:** данные берутся из Happ API `list-install` по `install_code` из ссылки. Если ссылка без `installid`, счётчик не работает.
- **Подсказки в мини-аппе:** при 0 устройств показывается текст про задержку 1–2 минуты и обновление экрана; при ссылке без installid — «получить ссылку в боте (Мой конфиг)».
- **API:** если в БД сохранён базовый URL без `installid`, он не считается готовой ссылкой — API пробует сгенерировать полную Happ-ссылку и сохранить. В ответ добавлено поле `subscription_link_has_install_limit`.
- **Fallback лимита:** когда Happ не вернул счётчик, но есть ссылка с installid, подставляется лимит из тарифа (показ 0/N).
- **Happ API:** во все запросы к Happ добавлен заголовок `Accept: application/json`. Дефолтный URL заменён с `https://happ-proxy.com` на **`https://api.happ-proxy.com`** (рабочий адрес).
- **Файлы изменены:**  
  `api_miniapp.py`, `bot/utils/happ_client.py`, `bot/config/settings.py`, `bot/handlers/main.py`,  
  `webapp/index.html`, `index.html`, `public/index.html`, `api/root_index.html`,  
  `.env.example`, `ПРОВЕРКА_ССЫЛКИ_HAPP.md`.
- **На сервере пользователь должен:** в `/opt/vpn-bot/.env` выставить `HAPP_API_URL=https://api.happ-proxy.com`, перезапустить `miniapp-api` (и бота при необходимости).

### Возможные ошибки / на что смотреть дальше

- Если после смены URL на сервере ссылка в мини-аппе по-прежнему без `installid` — проверить, что в `.env` на сервере действительно `HAPP_API_URL=https://api.happ-proxy.com` и сервисы перезапущены.
- Старые подписки в БД могли сохранить базовый URL в `vpn_config`; при следующем открытии мини-апп попытается выдать новую ссылку с installid (если API доступен).
- Домен подписки (95.181.175.67) может требовать привязки в Happ через `add-domain` (domain_hash) — при проблемах с лимитом уточнять в поддержке Happ.

### Идеи для следующих сессий

- При новых багах или доработках сначала смотреть этот журнал и список изменённых файлов выше, чтобы не ломать уже сделанное.
- Крупные фичи или рефакторинг — добавлять в журнал план и потом краткий отчёт.

---

### Дополнение: бот выдаёт ссылку с installid, в аппе всё равно 0 устройств

**Запрос:** Ссылка вида `...?installid=A8u9D1HjG3OA` приходит, но в разделе «Устройства» по-прежнему 0.

**Сделано:**
- Сопоставление `install_code` в `get_install_stats` сделано без учёта регистра (Happ может вернуть в другом регистре).
- Если запись по install найдена, но в ответе нет `install_count`, возвращаем 0 и лимит из ответа (чтобы показывать 0/N).
- Подсказка в мини-аппе уточнена: «Счётчик показывает, сколько устройств **добавили эту ссылку в приложение Happ** (Подписки → +). Если только что добавили — подождите 1–2 минуты и обновите экран.»
- В `happ_client` добавлен debug-лог при успешном ответе list-install (count/limit), чтобы в логах было видно, что вернул Happ.

**Файлы:** `bot/utils/happ_client.py`, `webapp/index.html`, `index.html`, `public/index.html`, `api/root_index.html`.

**Почему может быть 0:** Счётчик в Happ растёт, когда **устройство добавляет эту ссылку в приложение Happ** (Подписки → +). Пока ссылка только скопирована/получена в боте, но не добавлена в Happ на телефоне/ПК — Happ отдаёт 0. После добавления подписки в Happ и подключения обновление счётчика может занять 1–2 минуты. Если через несколько минут по-прежнему 0 — смотреть логи API (уровень debug): нашли ли мы install в list-install и какие count/limit пришли.

---

### Защита ссылки: разные ссылки, чтобы нельзя было стереть installid и пользоваться бесплатно

**Запрос:** Реальная ссылка `https://95.181.175.67:2096/sub_bitvpn/bgmdn1s016p08yfb` + installid; пользователь может удалить всё после `?installid` и использовать базовый URL бесплатно.

**Сделано:**
- Добавлен **редирект через API**: эндпоинт `GET /sub/{install_code}` отдаёт 302 на `HAPP_SUBSCRIPTION_URL?installid=CODE`. Пользователь получает ссылку вида `https://ваш-домен-api/sub/XXXXXXXXXXXX` — реальный URL подписки не показывается; без кода запрос к `/sub/` даёт 404.
- В `.env` опция **HAPP_SUBSCRIPTION_REDIRECT_BASE** (например `https://bitecosystem.ru`). Если задана — бот и API выдают ссылку в формате `REDIRECT_BASE/sub/КОД` вместо прямой ссылки на подписку.
- В `parse_install_code_from_happ_link` добавлена поддержка формата `/sub/CODE` (извлечение кода из пути для счётчика устройств).
- Обновлены: создание подписки при оплате (бот и api_miniapp), выдача ссылки в «Мой конфиг», мини-апп; везде при наличии REDIRECT_BASE сохраняется и отдаётся редирект-ссылка.

**Файлы:** `api_miniapp.py`, `bot/utils/happ_client.py`, `bot/config/settings.py`, `bot/handlers/main.py`, `.env.example`, `HAPP_НАСТРОЙКА.md`, новый `HAPP_ЗАЩИТА_ССЫЛКИ.md`.

**Что сделать на сервере:** В `.env` добавить `HAPP_SUBSCRIPTION_REDIRECT_BASE=https://ваш-домен-api` (тот же домен, что у API), перезапустить API и бота. Новые ссылки будут в формате `/sub/КОД`.

---

### Ревизия папок и файлов: удалено лишнее, обновлены ссылки

**Запрос:** Проверить все папки и файлы, правильность значений, удалить ненужное и уже сделанное.

**Удалено:**
- **ИСПРАВИТЬ_404_СЕЙЧАС.txt**, **ОШИБКА_404_ПОСЛЕ_ГИТ.txt**, **ПРОВЕРКА_404_ПОШАГОВО.txt** — инструкции по 404 дублировались; краткий блок «Если 404 в Telegram» перенесён в **ПРОВЕРКА_ПОСЛЕ_ДЕПЛОЯ.md**.
- **ОДНА_ССЫЛКА_ПОШАГОВО.txt**, **ЧТО_СДЕЛАТЬ_САМОМУ_ПОШАГОВО.txt** — устаревшие (MINIAPP_API_URL=vercel; сейчас API на своём сервере).
- **HAPP_КАК_НАСТРОИТЬ_ССЫЛКУ.md** — дублировал **HAPP_НАСТРОЙКА.md**; в коде ссылка заменена на HAPP_НАСТРОЙКА.md.
- Дубликаты 3D-моделей из **корня** проекта: **gaming_laptop.glb**, **apple_iphone_15_pro_max_black.glb**, **samsung_galaxy_s22_ultra.glb**, **tux.glb**, **macbook_pro_14-inch_m5.glb** (оставлены в **webapp/** и **public/**).
- **iphone_15_pro_max.zip** — архив не использовался в коде.

**Обновлено:**
- **ПРОВЕРКА_ПОСЛЕ_ДЕПЛОЯ.md** — в начало добавлен раздел «Если 404 в Telegram» (BotFather, WEBAPP_URL, перезапуск).
- **СЕРВЕР_ПОСЛЕ_ГИТ_ВОССТАНОВИТЬ.txt**, **СЕРВЕР_НАСТРОИТЬ_ГИТ_ОДИН_РАЗ.txt** — ссылки на удалённые файлы заменены на ПРОВЕРКА_ПОСЛЕ_ДЕПЛОЯ.md и корректные имена скриптов (ДЕПЛОЙ_НА_СЕРВЕР.bat).

**Оставлено без изменений:** КНОПКИ_И_СКРИПТЫ.txt, ОШИБКА_ПРИ_START.txt, СБП_ПОЧЕМУ_НЕ_ВИДНО.txt, ОПЛАТА_МИНИ_АПП_ОШИБКА.txt, ПРОВЕРКА_ОПЛАТЫ_НА_СЕРВЕРЕ.txt, ЮKASSA_ИНТЕГРАЦИЯ.txt, demo_bot.py, demo_interface.py (могут пригодиться для проверки), все .bat и .vbs скрипты деплоя/запуска.

---

### Структура docs/ и компактные папки

**Запрос:** Пройтись по всем файлам, удалить/объединить дубликаты, сделать компактнее по папкам.

**Сделано:**
- Создана папка **docs/** с подпапками: **happ/** (Happ), **deploy/** (деплой, сервер, Vercel), **payments/** (оплата), **miniapp/** (мини-апп), **other/** (скрипты, ошибки, журнал).
- Вся документация (.md и .txt) перенесена из корня в соответствующие подпапки docs/.
- Объединены **ЗАПУСК.md** и **КАК_ЗАПУСТИТЬ_ВСЁ.txt** в один файл **docs/deploy/Запуск_и_первый_деплой.md**.
- В корне остались: код, .env.example, README.md, deploy_config.cmd, все **.bat** и **.vbs** (удобно запускать из корня).
- Добавлено оглавление **docs/README.md** со ссылками на все разделы. В корневой README добавлена ссылка на docs/.
- Правило Cursor обновлено: журнал читать из **docs/other/SESSION_JOURNAL.md**.

**Файлы:** созданы docs/, docs/README.md, docs/deploy/Запуск_и_первый_деплой.md; перемещены все .md и .txt в docs/*; обновлены .cursor/rules/session-journal.mdc, README.md.

---

### Ссылка не изменилась и счётчик устройств не отображается

**Запрос:** Ссылка по-прежнему `https://95.181.175.67:2096/sub_bitvpn/...?installid=A8u9D1HjG3OA`, не перешла на редирект; количество подключённых устройств не показывается.

**Сделано:**
- **Перезапись старых ссылок в API:** если в БД сохранена ссылка с `installid=` (прямой URL), а в `.env` задан **HAPP_SUBSCRIPTION_REDIRECT_BASE**, при запросе `/api/miniapp/me` ссылка перезаписывается в формат редиректа (`REDIRECT_BASE/sub/КОД`) и сохраняется в БД. Добавлен импорт Config в этом блоке.
- **INFO-логирование get_install_stats:** в api_miniapp при вызове счётчика устройств пишется в лог: какой `api_url` используется, `install_code` (первые 6 символов), результат (used/limit или not_found). Исключения логируются как warning. По логам на сервере видно, что вернул Happ и нашли ли install.
- **Документация:** в **docs/happ/ПРОВЕРКА_ССЫЛКИ_HAPP.md** везде заменён URL на `https://api.happ-proxy.com`; добавлен **раздел 6** «Счётчик «Подключено» не показывается» — шаги: HAPP_API_URL на сервере, перезапуск API, просмотр логов get_install_stats, ручная проверка list-install через curl.

**Файлы:** `api_miniapp.py`, `docs/happ/ПРОВЕРКА_ССЫЛКИ_HAPP.md`.

**Что проверить на сервере:**
1. В `.env`: `HAPP_SUBSCRIPTION_REDIRECT_BASE=https://николай.lisobyk.fvds.ru` (или ваш домен API) и `HAPP_API_URL=https://api.happ-proxy.com`.
2. Перезапуск: `sudo systemctl restart miniapp-api` (и бота).
3. Открыть мини-апп → «Устройства», затем в логах API искать строку `miniapp_me: get_install_stats ...` — по ней видно, нашёлся ли install и какие used/limit вернул Happ.

---

### Git: ветка main (локально и на GitHub)

**Запрос:** Запомнить и записать в журнал, чтобы не давать пользователю неверные инструкции (в т.ч. по git push).

**Контекст:**
- В репозитории VPN BOT везде используется ветка **main** (локально и на GitHub, в деплое указана main).
- Если `git push` выдаёт ошибку про upstream — значит upstream не задан или указывает на другую ветку.

**Правильные команды для push (указывать в инструкциях):**
- Отправить изменения в удалённую ветку main:
  ```cmd
  git push origin main
  ```
- Чтобы в дальнейшем достаточно было `git push`, один раз задать upstream:
  ```cmd
  git branch --set-upstream-to=origin/main main
  ```

**Правило для агента:** В инструкциях по деплою писать **`git push origin main`**. Не предлагать push в master — в этом проекте рабочая ветка main.

---

### Имя и описание подписки в Happ (как у LastdepVPN, без домена)

**Запрос:** Чтобы в Happ у всех отображалось только название VPN (без цифр), сервер — 95.181.175.67; плюс описание (поддержка, бот, тарифы). Домена нет.

**Сделано:**
- В контенте подписки при прокси через `/sub/КОД` подменяются поле **ps** (remark) в ссылках vmess/vless/trojan/ss на заданные имя и описание. Подключение остаётся к серверу из нод (95.181.175.67).
- Переменные в `.env`: **SUBSCRIPTION_DISPLAY_NAME** (по умолчанию "BIT VPN"), **SUBSCRIPTION_DESCRIPTION** (многострочно; в .env `\n` — новая строка).
- Функция `_rewrite_subscription_remark()` в api_miniapp: декодирует base64 подписки, правит vmess (json ps) и vless/trojan/ss (fragment ps=), кодирует обратно.

**Условие:** Должен быть задан **HAPP_SUBSCRIPTION_REDIRECT_BASE**, чтобы пользователи получали ссылку вида `https://ваш-API/sub/КОД`; тогда контент идёт через наш API и подмена применяется.

**Файлы:** api_miniapp.py, bot/config/settings.py, .env.example, docs/happ/HAPP_НАСТРОЙКА.md.

---

## 2026-03-17 (вторник)

### Запросы пользователя

1. **Сделать главный экран как на макете (скрин)**  
   Большая кнопка «Оформить подписку», две кнопки «Устройства/Помощь» и 3D‑устройство с вращением прямо на главном экране (как в модалке).

2. **Ревизия проекта: синхронизация бота и мини‑аппа, возможные ошибки**

### Что сделано

- **UI главного экрана:** перенесён 3D‑блок устройства (three.js + `.glb`) на главный экран; обновлены стили карточки/кнопок под макет; “Установить подписку” на главном скрыта.
- **3D на главной:** добавлен автозапуск/остановка 3D при переключении вкладок и выбор модели по платформе (iOS/Android/Windows/macOS/Linux).
- **Синхронизация HTML копий:** те же правки применены в `webapp/index.html`, `public/index.html`, `api/root_index.html`, `index.html`, чтобы не расходились версии в разных способах деплоя.

### Файлы изменены

- `webapp/index.html`
- `public/index.html`
- `api/root_index.html`
- `index.html`

### Что проверить / возможные проблемы

- В мини‑аппе открыть **Главная** и убедиться, что 3D‑устройство крутится и не “зависает” после переходов по нижнему меню.
- Если 3D не загрузился: возможно блокируется загрузка модулей three.js с CDN (unpkg/esm.sh) или не находятся `.glb` файлы в текущем месте раздачи.

*Ниже добавлять новые сессии по тому же шаблону (дата, запросы, что сделано, файлы, риски/проверки).*

---

## 2026-03-19 (четверг)

### Запрос пользователя
- «Запомни просто и запиши себе что я тебе написал и что ты мне ответил, позже вернемся к этому».
- В предыдущем контексте: пользователь привёл ответы Грока про VPN-инфраструктуру (отличие серверов, как ускорять скорость, DDoS/защита, выбор городов, “без рекламы”, резервные серверы, архитектура безопасности, рост до 1000 пользователей) и попросил: «проанализируй все, не торопись и скажи что думаешь и какие улучшения можешь предложить».

### Что ответил я (кратко)
- Сказал, что советы Грока в целом практичны: WireGuard, низкая задержка, DDoS-защита, мульти-регион, мониторинг/бэкапы, резервные серверы и усиление реферального роста.
- Предложил улучшения, привязанные к вашему проекту: усилить документацию по архитектуре/авариям, продумать модель резервов (скрытые в обычном режиме, включаемые при падении), чек-листы деплоя и сценарии “если всё упало”, усилить реферальную механику и мониторинг.

### Файлы
- `docs/other/SESSION_JOURNAL.md` (вот эта запись).

---

## 2026-03-19 (четверг) — iOS /sub

### Запрос пользователя
- Как перезапустить `miniapp-api` на сервере.
- Почему iPhone в Happ отклоняет подписку по ссылке `/sub/...`.
- Как скрыть отображаемый серверный IP в ссылке и защитить ссылку от правок/абуза.

### Что сделано
- В `api_miniapp.py` усилена обработка `/sub/{install_code}`:
  - добавлена защита перепаковки тела подписки (не ломаем upstream, если формат не похож на подписку);
  - добавлены заголовки для iOS (явный `Content-Type`, `Content-Disposition`, `providerid`).

### Файлы
- `api_miniapp.py`

---

## 2026-03-19 (четверг) — автодобавление в Happ

### Запрос пользователя
- Сделать так, чтобы в мини-аппе при нажатии кнопки добавления подписки/устройства происходил автоматический переход в Happ Proxy Client и подписка добавлялась без копирования ссылки вручную (как у конкурентов через `happ://add/...`).

### Что сделано
- В фронтенде обработчик кнопки `+ Добавить устройство` в экране «Устройства» теперь открывает deep-link `happ://add/<subscription_url>` вместо копирования ссылки.
- В `api_miniapp.py` обновлён текст подсказки `devices_hint`, чтобы он соответствовал новой логике (кнопка сама откроет Happ).

### Файлы
- `public/index.html`, `webapp/index.html`, `index.html`, `api/root_index.html`
- `api_miniapp.py`

---
## 2026-03-19 (четверг) — deep-link автодобавление

### Запрос пользователя
- На iPhone и на ноутбуке появляется та же ошибка: Happ не открылся автоматически (показывается fallback-алерт).

### Что сделано
- Во всех 4 копиях фронтенда обновлена `openHappAddSubscription`:
  - deep-link теперь открывается через `tg.openLink` (если доступно), но deep-link формируется в нескольких вариантах (`happ://add/<raw>`, `encodeURI`, `encodeURIComponent`, и через `happ://add?url=`), чтобы попасть в формат, который понимает Happ.
  - fallback-алерт теперь показывается только если за ~4 сек страница не ушла в фон (чтобы не показывать алерт, когда Happ уже открылся, но iOS открывает медленнее).
  - по fallback в буфер копируется исходный `subUrl`, чтобы вручную было быстро.

### Файлы
- `public/index.html`
- `webapp/index.html`
- `index.html`
- `api/root_index.html`

---
## 2026-03-19 (четверг) — автоподсказка при неудаче deep-link
### Что сделано
- В `openHappAddSubscription()` во всех 4 копиях: после попытки автозапуска Happ проверяем `document.hidden`; если за ~0.95с страница не ушла в background, показываем `showLinkCopiedModal(subUrl)`, чтобы пользователь мог вручную вставить ссылку в Happ без “синего экрана” редиректа.

### Файлы
- `public/index.html`
- `webapp/index.html`
- `index.html`
- `api/root_index.html`

---
## 2026-03-19 (четверг) — автовозврат и автодобавление в Happ
### Что сделано
- В `api_miniapp.py` обновлена страница `redirect-to-app`: добавлен параметр `return_url` и авто-возврат обратно на страницу мини-аппа; возврат не выполняется, если страница ушла в `hidden` (признак что Happ открылся).
- Во всех 4 копиях фронтенда `openHappAddSubscription` теперь передаёт `return_url` в ссылку `redirect-to-app`.
- В экране «Установка клиента» кнопки `Добавить подписку` и `Установить` теперь запускают автодобавление в Happ через `openHappAddSubscription`, а не открывают промежуточное меню/подтверждение.

### Файлы изменены
- `api_miniapp.py`
- `public/index.html`
- `webapp/index.html`
- `index.html`
- `api/root_index.html`

### Что проверить
- На iPhone: исчез ли “синий экран” редирект-страницы и возвращается ли мини-апп сразу.
- Убедиться, что Happ действительно получает добавление подписки/лимит и счётчик устройств начинает обновляться (может зависеть от корректности `HAPP_SUBSCRIPTION_REDIRECT_BASE`).

---
## 2026-03-19 (четверг) — deep-link метод открытия (iOS)

### Причина
- На iOS/в Telegram WebApp deep-link может не открываться, если полагаться на `tg.openLink` для кастомной схемы.

### Что сделано
- В `openHappAddSubscription()` (во всех 4 HTML) поменял порядок попыток открытия:
  - сначала `window.location.href = deep`
  - затем клик по скрытому `<a href=...>`
  - `tg.openLink(deep)` оставлен как запасной вариант

### Файлы
- `public/index.html`
- `webapp/index.html`
- `index.html`
- `api/root_index.html`

### Что проверить / возможные риски
- Повторить автодобавление в Happ с iPhone и ноутбука: окно Happ должно появляться, а fallback-алерт “вставьте ссылку вручную” — не должен срабатывать при успешном открытии.
- После добавления подписки проверить, что счётчик устройств обновился (через `/debug-install-stats` на API).

---
## 2026-03-19 (четверг) — deep-link формат (happ://crypto)

### Причина
- В доках Happ зашифрованные подписки начинаются с `happ://crypto...` и их обычно нужно открывать в Happ напрямую, а не заворачивать в `happ://add/<url>`.

### Что сделано
- В `openHappAddSubscription()` во всех 4 HTML добавлена проверка:
  - если `subscription_link` уже начинается с `happ://`, то открываем его напрямую (без `happ://add/...`).
  - иначе используем варианты `happ://add/...` + `happ://add?url=...`.

### Файлы
- `public/index.html`
- `webapp/index.html`
- `index.html`
- `api/root_index.html`

---
## 2026-03-19 (четверг) — deep-link fallback без alert + clipboard

### Причина
- На iOS Telegram WebApp deep-link может не открываться, но пользователю мешает показ alert; также блок со ссылкой в “Устройствах” может не отображаться.

### Что сделано
- Во всех 4 HTML в `openHappAddSubscription()` перед попыткой deep-link выполняем `navigator.clipboard.writeText(subUrl)`.
- Убрали `tg.showAlert(...)` в fallback: теперь при неудаче показывается только модалка `showLinkCopiedModal(subUrl)`.
- Добавили дополнительные кандидаты deep-link без параметров: `happ://add`, `happ://open`, `happ://` (на случай импорта из clipboard).

### Файлы
- `public/index.html`
- `webapp/index.html`
- `index.html`
- `api/root_index.html`

---
## 2026-03-19 (четверг) — deep-link только "happ://add/<url>"

### Причина
- На iOS/Telegram WebApp deep-link не запускается: не появляется системное окно “Открыть Happ Proxy Client?”.
- Дополнительные кандидаты deep-link и предварительные действия (clipboard) могли ломать user-gesture.

### Что сделано
- Во всех 4 HTML:
  - `openHappAddSubscription()` теперь использует только формат `happ://add/<subscription_url>` (и запасной вариант с `encodeURIComponent`).
  - попытка второго deep-link делается сразу (без `setTimeout`).
  - предварительная запись `subscription_link` в clipboard убрана.
  - обработчик кнопки `+ Добавить устройство` берёт `subscription_link` не только из `sheet-happ-link-value`, но и из `API_ME.subscription.subscription_link` при пустом значении.

### Файлы
- `public/index.html`
- `webapp/index.html`
- `index.html`
- `api/root_index.html`

---
## 2026-03-19 (четверг) — редирект через браузер

### Причина
- iOS/Telegram WebApp не запускает `happ://...` напрямую (не появляется системное окно “Открыть Happ Proxy Client?”).

### Что сделано
- В `openHappAddSubscription()` (во всех 4 HTML копиях) теперь открывается HTTPS-страница
  `.../api/miniapp/redirect-to-app?url=<happ-deeplink>`.
- Эта страница в `api_miniapp.py` выполняет `window.location.replace(target)` на `happ://...`, т.е. deep-link триггерится из браузера.

### Файлы
- `public/index.html`
- `webapp/index.html`
- `index.html`
- `api/root_index.html`

---
## 2026-03-19 (четверг) — усиление redirect-to-app скрипта

### Причина
- Открывается страница `redirect-to-app`, но автоматический `happ://...` запуск не срабатывает, появляется fallback “Открыть вручную”.

### Что сделано
- В `api_miniapp.py` усилен HTML `REDIRECT_TO_APP_HTML`:
  - пробуем открыть target сразу через `replace`, `href`, `assign`;
  - дополнительно делаем клик по скрытой ссылке;
  - fallback показываем быстрее (1.6 сек).

### Файлы
- `api_miniapp.py`

---
## 2026-03-19 (четверг) — iOS: подписка добавлена, но содержимое не получено

### Симптомы
- В iPhone Safari/поведение Happ: “Сертификат недействителен” для `https://213.165.38.222/...`.
- В Happ: “Подписка добавлена, но получить содержимое не удалось. Попробуйте обновить вручную.”
- Счётчик устройств остаётся `0/10`.

### Причина
- Happ на iOS не смог скачать содержимое подписки по HTTPS, т.к. используется IP (`213.165.38.222`), а сертификат TLS невалиден для этого имени/IP.

### Что сделано
- В `api_miniapp.py` добавлена нормализация всех ссылок вида `/sub/<code>` под текущий `redirect_base` (hostname с валидным SSL) и сборка ссылки в формате:
  `https://<redirect_base>/sub/<code>?installid=<code>`
- Это должно убрать TLS-ошибку на iOS и включить корректный учёт `installid`.

### Файлы
- `api_miniapp.py`

---
## 2026-03-19 (четверг) — iOS: нормализация /sub под домен из конфига

### Причина
- После попытки с `?installid=` iPhone снова получает ссылку на IP `https://213.165.38.222/...` и Happ не скачивает содержимое.

### Что сделано
- В `api_miniapp.py` в блоке нормализации `/sub/<code>` изменена логика:
  - раньше редирект-base брали из Host запроса (может быть IP);
  - теперь редирект-base берём из `HAPP_SUBSCRIPTION_REDIRECT_BASE` (домен с валидным SSL), и только если его нет — fallback на Host запроса.

### Файлы
- `api_miniapp.py`

---
## 2026-03-19 (четверг) — показать ссылку при fallback

### Причина
- На экране “Устройства” у пользователя не отображается блок со ссылкой и “Скопировать”.

### Что сделано
- В `openHappAddSubscription()` при срабатывании fallback теперь вызывается `showLinkCopiedModal(subUrl)`, чтобы появилась модалка с копированием `subscription_link`, даже если в “Устройствах” её не видно.

### Файлы
- `public/index.html`
- `webapp/index.html`
- `index.html`
- `api/root_index.html`

---
## Задачи дня — бот, устройства, crypto-ссылка, документация по серверам

### Запрос пользователя
- Сменить сервер бота и API; показать количество устройств в боте; закупка VPS (DE «без рекламы», LTE); зашифровать ссылку для Happ чтобы не светился IP.

### Что сделано в коде
- **Устройства в боте:** общая логика Happ `list-install` в `_happ_devices_html_line()`; учёт `HAPP_LIST_INSTALL_URL`; строка в **профиле**, в **/start** и **главном меню** при активной подписке.
- **Зашифрованная ссылка для мини-аппа:** `happ_client.encrypt_subscription_url_to_crypto()` → POST `https://crypto.happ.su/api-v2.php`; в `api_miniapp.py` при `HAPP_ENCRYPT_SUBSCRIPTION_LINKS=true` в ответе `/api/miniapp/me` поле `subscription_link` — `happ://crypt*` (HTTPS для расчёта счётчика внутри не меняется в БД).
- **Документация:** `docs/deploy/СМЕНА_СЕРВЕРА_БОТ_И_MINIAPP.md`, `docs/infra/ЗАМЕТКИ_YOUTUBE_И_LTE.md`; обновлены `docs/README.md`, `.env.example`.

### Файлы
- `bot/handlers/main.py`, `bot/utils/happ_client.py`, `api_miniapp.py`, `.env.example`, `docs/README.md`, `docs/deploy/СМЕНА_СЕРВЕРА_БОТ_И_MINIAPP.md`, `docs/infra/ЗАМЕТКИ_YOUTUBE_И_LTE.md`

### Что проверить
- Включить на сервере `HAPP_ENCRYPT_SUBSCRIPTION_LINKS=true`, перезапустить miniapp-api; в мини-аппе подписка открывается в Happ, счётчик устройств живой.
- После смены VPS: `WEBAPP_URL`, `MINIAPP_API_URL`, `HAPP_SUBSCRIPTION_REDIRECT_BASE`, BotFather, перезапуск сервисов.

---
## Установка клиента: «Добавить подписку» — снова модалка с копированием

### Запрос
- Вернуть поведение белой кнопки «Добавить подписку»: не автопереход в Happ, а окно с инструкцией и «Скопировать ссылку».

### Сделано
- Во всех 4 копиях `index.html` обработчик `platform-btn-add-sub` снова вызывает `showLinkCopiedModal` (+ копирование в буфер), без `openHappAddSubscription`.
- Кнопка «Установить» в листе подтверждения снова использует `openHappLink` (копирование + модалка, на Android запуск оболочки Happ), без `openHappAddSubscription`.

### Файлы
- `public/index.html`, `webapp/index.html`, `index.html`, `api/root_index.html`

---
## Карточка «Моя подписка» после оплаты + кнопка в мини-аппе

### Запрос
- После оплаты сообщение со ссылкой, сроком, кнопками «Подключиться», «Мои устройства (x/y)», «Назад»; в разделе «Подписки» кнопка «Моя подписка» → бот с `start=my_subscription`.

### Сделано
- `bot/utils/subscription_card.py`, локали `my_subscription_card*`, отправка карточки в `verify_payment` и `api_miniapp._complete_payment_and_send_link`; `/start my_subscription`; callback `my_sub_refresh`, `my_sub_connect`; кнопка в 4× `index.html`.

### Файлы
- `bot/utils/subscription_card.py`, `bot/handlers/main.py`, `bot/main.py`, `api_miniapp.py`, `locales/ru.py`, четыре `index.html`

---
## Кнопка «Моя подписка» в мини-аппе не открывала бота

### Причина
- В `index.html` в `botUsername` подставлялся первый сегмент `start_param` до `_`. Для `my_subscription` получалось `my` → ссылка `t.me/my?start=...` (неверно), `openTelegramLink` ничего не делал.

### Сделано
- Убрано присвоение username из `start_param`; оставлен только referrer `t.me/...` + дефолт/API.
- Для кнопки добавлены fallback: `openLink`, `location.href`, `window.open`.

### Файлы
- `public/index.html`, `webapp/index.html`, `index.html`, `api/root_index.html`, `docs/other/SESSION_JOURNAL.md`

---
## 404 на GET /health за nginx

### Причина
- Nginx проксирует на miniapp-api только `/api/` и `/sub/`; `GET /health` попадает в `location /` (статика) → 404.

### Сделано
- В `api_miniapp.py` добавлен `GET /api/health` (тот же ответ, что `/health`). Проверка: `https://домен/api/health`.

### Файлы
- `api_miniapp.py`, `deploy/nginx-miniapp-api.conf`, `docs/deploy/СМЕНА_СЕРВЕРА_БОТ_И_MINIAPP.md`, `docs/other/SESSION_JOURNAL.md`

---
## Долгий ответ после «Моя подписка»

### Причина
- При сборке карточки вызывался Happ `list-install` (синхронный HTTP до ~10 с), блокировал ответ на `/start my_subscription`.

### Сделано
- `build_my_subscription_card(..., fetch_device_counts=False)` для deep link и после оплаты (бот + webhook); лимит из тарифа, в кнопке «—/N» до обновления. Полный счётчик — по callback `my_sub_refresh` (как раньше).

### Файлы
- `bot/utils/subscription_card.py`, `bot/handlers/main.py`, `api_miniapp.py`, `docs/other/SESSION_JOURNAL.md`

---
## Деплой «одной кнопкой»: сервер и Vercel

### Запрос
- Один скрипт — весь деплой на VPS; второй — весь деплой мини-аппа (Vercel).

### Сделано
- `deploy_config.cmd`: `RESTART_MINIAPP_CMD`; `ДЕПЛОЙ_НА_СЕРВЕР.bat` использует его вместо хардкода.
- `ДЕПЛОЙ_МИНИАПП_VERCEL.bat`: webapp→public→index→api, git push, `npx vercel --prod`; режим `KEEPOPEN` + отдельное окно при двойном клике; `ДЕПЛОЙ_ВСЁ.bat` вызывает с `KEEPOPEN`.
- `README_ДЕПЛОЙ.txt`, `Запустить_деплой_миниапп_Vercel.vbs` (позже дубли bat убраны — см. блок «Деплой без дублей»).
- Обновлены `docs/other/КНОПКИ_И_СКРИПТЫ.txt`, `docs/deploy/Запуск_и_первый_деплой.md`.

### Файлы
- `deploy_config.cmd`, `ДЕПЛОЙ_НА_СЕРВЕР.bat`, `ДЕПЛОЙ_МИНИАПП_VERCEL.bat`, `ДЕПЛОЙ_ВСЁ.bat`, `README_ДЕПЛОЙ.txt`, `Запустить_деплой_миниапп_Vercel.vbs`, `docs/other/SESSION_JOURNAL.md`

---
## Деплой без дублей, шифрование ссылок, счётчик

### Запрос
- Объединить дублирующиеся bat; проверить деплои; шифровать выдаваемые HTTPS `/sub/...`; счётчик устройств в боте и мини-аппе.

### Сделано
- Удалены `ДЕПЛОЙ_СЕРВЕР_ОДНА_КНОПКА.bat`, `ДЕПЛОЙ_МИНИАПП_ОДНА_КНОПКА.bat`; vbs → `ДЕПЛОЙ_МИНИАПП_VERCEL.bat`; README_ДЕПЛОЙ и доки обновлены.
- `ДЕПЛОЙ_МИНИАПП_API_ТОЛЬКО.bat`: `RESTART_MINIAPP_CMD` из deploy_config; заголовок про отличие от полного серверного деплоя.
- `Config.HAPP_ENCRYPT_SUBSCRIPTION_LINKS`; `subscription_card`: `link_for_user_display`, кнопка «Подключиться» через crypto deep link; файл после оплаты и `my_sub_connect` — crypto при включённом флаге; api webhook — `link_for_user_display` для .txt.
- `happ_client` list-install timeout 5 с; `.env.example` HAPP_ENCRYPT=true; `docs/happ/ЗАШИФРОВКА_ССЫЛКИ_КРАТКО.md`.

### Файлы
- `bot/config/settings.py`, `bot/utils/subscription_card.py`, `bot/handlers/main.py`, `api_miniapp.py`, `bot/utils/happ_client.py`, `.env.example`, `README_ДЕПЛОЙ.txt`, `ДЕПЛОЙ_МИНИАПП_API_ТОЛЬКО.bat`, `Запустить_деплой_миниапп_Vercel.vbs`, `docs/happ/ЗАШИФРОВКА_ССЫЛКИ_КРАТКО.md`, `docs/README.md`, `docs/other/КНОПКИ_И_СКРИПТЫ.txt`, `docs/deploy/Запуск_и_первый_деплой.md`, `docs/other/SESSION_JOURNAL.md`

---
## Бот долго отвечает / не реагирует на команды

### Причина
- `_happ_devices_html_line` вызывал синхронный **Happ list-install** при каждом `/start` и в главном меню — блокировал event loop (до ~5 с на запрос).
- **crypto.happ.su** при `HAPP_ENCRYPT_SUBSCRIPTION_LINKS` — таймаут 20 с на запрос при сборке карточки.

### Сделано
- `_happ_devices_html_line`: только лимит из тарифа, без HTTP; счётчик — в мини-аппе / кнопка «Мои устройства».
- `encrypt_subscription_url_to_crypto`: timeout **5** с.
- `Application.builder().concurrent_updates(True)`.

### Файлы
- `bot/handlers/main.py`, `bot/utils/happ_client.py`, `bot/main.py`, `docs/other/SESSION_JOURNAL.md`

---
## TimedOut при старте бота (getMe)

### Причина
- До `api.telegram.org` с VPS дефолтный таймаут httpx мал — `telegram.error.TimedOut`, сервис падает.

### Сделано
- `HTTPXRequest` с `TG_HTTP_TIMEOUT` (по умолчанию 30 с), переменная в `.env.example`.

### Файлы
- `bot/main.py`, `.env.example`, `docs/other/SESSION_JOURNAL.md`

### Если не помогло
- В логах **`httpcore.ConnectTimeout`** на **`start_tls`** — TLS до `api.telegram.org` не поднимается (часто блокировка/маршрут с датацентра, не «мало секунд»). Проверить: `curl -v --connect-timeout 15 https://api.telegram.org`.
- Задать **HTTPS-прокси** в `.env`: `TG_PROXY=http://...` или `HTTPS_PROXY=...`, перезапустить бота. В коде: один `HTTPXRequest` и для Bot API, и для **get_updates** (`get_updates_request`).
- `post_shutdown`: при **TimedOut** не дублировать ожидание и не писать лишний WARNING.

---
## Аудит безопасности и синхронизации (март 2026)

### Запрос
- Проверить папки, синхронизацию бот ↔ мини-апп, безопасность, GitHub, оплату.

### Сделано
- **ЮKassa webhook:** после `payment.succeeded` платёж проверяется через **API ЮKassa** (`verify_webhook_payment`: статус + сумма в копейках). `YooKassaPayment.get_payment` в `bot/utils/payments.py`.
- **api_miniapp:** CORS из **`MINIAPP_CORS_ORIGINS`**; **`API_EXPOSE_DOCS`** (по умолчанию docs выкл); отладочные маршруты только при **`MINIAPP_EXPOSE_DEBUG=1`**; rate limit **`MINIAPP_CREATE_PAYMENT_RATE`** на create-payment.
- **Деплой:** **`deploy_config.cmd`** в `.gitignore`; в репо **`deploy_config.example.cmd`**; **`ДЕПЛОЙ_НА_СЕРВЕР.bat`** и **`ПЕРЕЗАПУСК_НА_СЕРВЕРЕ.bat`** создают `deploy_config.cmd` из примера при отсутствии.
- Документация: **`docs/other/АУДИТ_ПРОЕКТА.md`** переписан; **`.env.example`** — новые переменные API.

### Файлы
- `bot/utils/payments.py`, `api_miniapp.py`, `.gitignore`, `deploy_config.example.cmd`, `README_ДЕПЛОЙ.txt`, `ДЕПЛОЙ_НА_СЕРВЕР.bat`, `ПЕРЕЗАПУСК_НА_СЕРВЕРЕ.bat`, `.env.example`, `docs/other/АУДИТ_ПРОЕКТА.md`, `docs/other/SESSION_JOURNAL.md`

### Что сделать на сервере после pull
- Скопировать **`deploy_config.example.cmd` → `deploy_config.cmd`**, указать **SERVER_IP** (локальный файл не в Git).
- В `.env` API: при проде задать **`MINIAPP_CORS_ORIGINS`**, оставить **`API_EXPOSE_DOCS=0`**, **`MINIAPP_EXPOSE_DEBUG=0`**.

---
## Ссылки /sub без дублирующего installid, список устройств в боте и мини-аппе

### Запрос
- Не светить `?installid=` в URL (код уже в пути `/sub/CODE`); список устройств как у конкурента — отключение; синхронизация бот ↔ мини-апп.

### Сделано
- **`happ_client.public_subscription_url`**, **`parse_install_code`**: сначала путь `/sub/`, потом query.
- **`list_hwids` / `delete_hwid`** (Happ API `list-hwid`, `delete-hwid`).
- **`api_miniapp`**: сохранение/нормализация ссылок без `?installid=`; в **`/api/miniapp/me`** поле **`subscription.devices`**; **`POST /api/miniapp/remove-device`** (initData + hwid).
- **Бот:** кнопка **«📱 Устройства (x/y)»** → **`hap_devices`**, экран списка + **`hap_d{N}`** отключить; **`my_sub_refresh`** — назад к карточке.
- **Мини-апп (`webapp/index.html` + копии):** список устройств из API, кнопка **✕**, после удаления — **`loadMeFromApi`**.

### Файлы
- `bot/utils/happ_client.py`, `api_miniapp.py`, `bot/utils/subscription_card.py`, `bot/handlers/main.py`, `bot/main.py`, `webapp/index.html`, `public/index.html`, `api/root_index.html`, `index.html`, `docs/other/SESSION_JOURNAL.md`
